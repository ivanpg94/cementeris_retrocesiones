services:
  cementeris_retrocesiones.services:
    class: Drupal\cementeris_retrocesiones\Services
    arguments: ['@logger.channel.default', '@messenger', '@cache.backend.database', '@current_user', '@http_client']
